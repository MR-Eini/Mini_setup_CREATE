# accordingly, so that your schedules always match the weather time series.
# So do not change your simulation period manually. Then the simulated
# years will not match the calculated schedules.
covcrop$write_operations(start_year = 1998, end_year = 2020)
# Update land use labels (should not be longer than 24 characters)
library(SWATdoctR)
update_landuse_labels(pth)
setwd("A:/Users/Eini/CS6/6_NSWRM/FarmR/Covcrop/2_FarmR")
#---------------------------------------------------
# Load the farmR
library(SWATfarmR)
library(tidyverse)
library(zoo)
#---------------------------------------------------
# Define the path to your SWAT project
pth <- 'A://Users//Eini//CS6//6_NSWRM//FarmR//Covcrop//clean_setup'
#---------------------------------------------------
# Load the farmR
library(SWATfarmR)
library(tidyverse)
library(zoo)
#---------------------------------------------------
# Define the path to your SWAT project
pth <- 'A://Users//Eini//CS6//6_NSWRM//FarmR//Covcrop//clean_setup'
# Define the path to your management schedule csv
mgt <- 'farmR_input_cc.csv'
new_farmr('covcrop', pth)
# Interpolate NA's in pcp data (if you have any)
covcrop$.data$variables$pcp[-1] <- na.approx(covcrop$.data$variables$pcp[-1])
# You can use by default the variables pcp, tmn, tmx, and tav, for dynamic
# rules in your management schedule. But you can also calculate and use new
# ones. You can also see in the example csv file how I used the variable
# in the dynamic rules. In this case I calculated the antecedent precipi-
# tation as a proxy for soil moisture using an exponential decay function
# (included in the R package). I used the pcp from the SWAT project for
# that which is stored in the farmR project.
api <- variable_decay(covcrop$.data$variables$pcp, -5,0.8)
# To add the variable to the farmR you have to tell it which variables are
# assigned to which HRUs for that I just copied the connections of pcp to
# HRUs and use the same for api
asgn <- select(covcrop$.data$meta$hru_var_connect, hru, pcp)
# You can add a variable to the farmR project with the internal function
# .$add_variable
covcrop$add_variable(api, "api", asgn, overwrite = T)
# Read your management table into your farmR project with .$read_management
covcrop$read_management(mgt, discard_schedule = TRUE)
# Read your management table into your farmR project with .$read_management
covcrop$read_management(mgt, discard_schedule = TRUE)
# Interpolate NA's in pcp data (if you have any)
covcrop$.data$variables$pcp[-1] <- na.approx(covcrop$.data$variables$pcp[-1])
# You can use by default the variables pcp, tmn, tmx, and tav, for dynamic
# rules in your management schedule. But you can also calculate and use new
# ones. You can also see in the example csv file how I used the variable
# in the dynamic rules. In this case I calculated the antecedent precipi-
# tation as a proxy for soil moisture using an exponential decay function
# (included in the R package). I used the pcp from the SWAT project for
# that which is stored in the farmR project.
api <- variable_decay(covcrop$.data$variables$pcp, -5,0.8)
# To add the variable to the farmR you have to tell it which variables are
# assigned to which HRUs for that I just copied the connections of pcp to
# HRUs and use the same for api
asgn <- select(covcrop$.data$meta$hru_var_connect, hru, pcp)
# You can add a variable to the farmR project with the internal function
# .$add_variable
covcrop$add_variable(api, "api", asgn, overwrite = T)
# Read your management table into your farmR project with .$read_management
covcrop$read_management(mgt, discard_schedule = TRUE)
# You can use by default the variables pcp, tmn, tmx, and tav, for dynamic
# rules in your management schedule. But you can also calculate and use new
# ones. You can also see in the example csv file how I used the variable
# in the dynamic rules. In this case I calculated the antecedent precipi-
# tation as a proxy for soil moisture using an exponential decay function
# (included in the R package). I used the pcp from the SWAT project for
# that which is stored in the farmR project.
api <- variable_decay(covcrop$.data$variables$pcp, -5,0.8)
# To add the variable to the farmR you have to tell it which variables are
# assigned to which HRUs for that I just copied the connections of pcp to
# HRUs and use the same for api
asgn <- select(covcrop$.data$meta$hru_var_connect, hru, pcp)
# You can add a variable to the farmR project with the internal function
# .$add_variable
covcrop$add_variable(api, "api", asgn, overwrite = T)
# Read your management table into your farmR project with .$read_management
covcrop$read_management(mgt, discard_schedule = TRUE)
# Read your management table into your farmR project with .$read_management
covcrop$read_management(mgt, discard_schedule = TRUE)
#---------------------------------------------------
# Load the farmR
library(SWATfarmR)
library(tidyverse)
library(zoo)
#---------------------------------------------------
# Define the path to your SWAT project
pth <- 'A://Users//Eini//CS6//6_NSWRM//FarmR//Covcrop//clean_setup'
# Define the path to your management schedule csv
mgt <- 'farmR_input_cc.csv'
#load_farmr(paste(pth, 'covcrop.farm', sep = '/'))
# Create a new farmR project. The new farmR will have the name 'farmR_ilm' in
# your R environment in this case. You can give it an individual name.
#covcrop.farm$reset_files()
new_farmr('covcrop', pth)
# Interpolate NA's in pcp data (if you have any)
covcrop$.data$variables$pcp[-1] <- na.approx(covcrop$.data$variables$pcp[-1])
# You can use by default the variables pcp, tmn, tmx, and tav, for dynamic
# rules in your management schedule. But you can also calculate and use new
# ones. You can also see in the example csv file how I used the variable
# in the dynamic rules. In this case I calculated the antecedent precipi-
# tation as a proxy for soil moisture using an exponential decay function
# (included in the R package). I used the pcp from the SWAT project for
# that which is stored in the farmR project.
api <- variable_decay(covcrop$.data$variables$pcp, -5,0.8)
# To add the variable to the farmR you have to tell it which variables are
# assigned to which HRUs for that I just copied the connections of pcp to
# HRUs and use the same for api
asgn <- select(covcrop$.data$meta$hru_var_connect, hru, pcp)
# You can add a variable to the farmR project with the internal function
# .$add_variable
covcrop$add_variable(api, "api", asgn, overwrite = T)
# Read your management table into your farmR project with .$read_management
covcrop$read_management(mgt, discard_schedule = TRUE)
# Read your management table into your farmR project with .$read_management
covcrop$read_management(mgt, discard_schedule = TRUE)
# Read your management table into your farmR project with .$read_management
covcrop$read_management(mgt, discard_schedule = TRUE)
# Interpolate NA's in pcp data (if you have any)
covcrop$.data$variables$pcp[-1] <- na.approx(covcrop$.data$variables$pcp[-1])
# You can use by default the variables pcp, tmn, tmx, and tav, for dynamic
# rules in your management schedule. But you can also calculate and use new
# ones. You can also see in the example csv file how I used the variable
# in the dynamic rules. In this case I calculated the antecedent precipi-
# tation as a proxy for soil moisture using an exponential decay function
# (included in the R package). I used the pcp from the SWAT project for
# that which is stored in the farmR project.
api <- variable_decay(covcrop$.data$variables$pcp, -5,0.8)
# To add the variable to the farmR you have to tell it which variables are
# assigned to which HRUs for that I just copied the connections of pcp to
# HRUs and use the same for api
asgn <- select(covcrop$.data$meta$hru_var_connect, hru, pcp)
# You can add a variable to the farmR project with the internal function
# .$add_variable
covcrop$add_variable(api, "api", asgn, overwrite = T)
# Read your management table into your farmR project with .$read_management
covcrop$read_management(mgt, discard_schedule = TRUE)
#---------------------------------------------------
# Load the farmR
library(SWATfarmR)
library(tidyverse)
library(zoo)
#---------------------------------------------------
# Define the path to your SWAT project
pth <- 'A://Users//Eini//CS6//6_NSWRM//FarmR//Covcrop//clean_setup'
# Define the path to your management schedule csv
mgt <- 'farmR_input_cc.csv'
new_farmr('covcrop', pth)
# Interpolate NA's in pcp data (if you have any)
covcrop$.data$variables$pcp[-1] <- na.approx(covcrop$.data$variables$pcp[-1])
# You can use by default the variables pcp, tmn, tmx, and tav, for dynamic
# rules in your management schedule. But you can also calculate and use new
# ones. You can also see in the example csv file how I used the variable
# in the dynamic rules. In this case I calculated the antecedent precipi-
# tation as a proxy for soil moisture using an exponential decay function
# (included in the R package). I used the pcp from the SWAT project for
# that which is stored in the farmR project.
api <- variable_decay(covcrop$.data$variables$pcp, -5,0.8)
# To add the variable to the farmR you have to tell it which variables are
# assigned to which HRUs for that I just copied the connections of pcp to
# HRUs and use the same for api
asgn <- select(covcrop$.data$meta$hru_var_connect, hru, pcp)
# You can add a variable to the farmR project with the internal function
# .$add_variable
covcrop$add_variable(api, "api", asgn, overwrite = T)
# Read your management table into your farmR project with .$read_management
covcrop$read_management(mgt, discard_schedule = TRUE)
# Study your management inputs
sched <- covcrop$.data$management$schedule
unique(sched$op_data1)
hru_attributes <- covcrop$.data$meta$hru_attributes
View(hru_attributes)
# You can now schedule operations which are based on your variables (pcp, etc.)
# and the rules that you defined in your management table. If no dates are
# defined the entire time series of your variables is used.
#farmR_ilm$schedule_operations(start_year = 2003, end_year = 2014, n_schedule=1) #n_schedule=1, => für jede landuse pro routing unit nur einen schedule in management.sch schreiben und für andere dann übernehmen)
covcrop$schedule_operations(start_year = 2001, end_year = 2021, replace = 'all')
#---------------------------------------------------
# Load the farmR
library(SWATfarmR)
library(tidyverse)
library(zoo)
#---------------------------------------------------
# Define the path to your SWAT project
pth <- 'A://Users//Eini//CS6//6_NSWRM//FarmR//Covcrop//clean_setup'
# Define the path to your management schedule csv
mgt <- 'farmR_input_cc.csv'
load_farmr(paste(pth, 'covcrop.farm', sep = '/'))
View(covcrop)
covcrop[[".data"]][["management"]][["schedule"]][["land_use"]]
#### Probelm in CS5 and CS6: a lot of duplicates should be removed
# extract schedule
sched1 <- covcrop[[".data"]][["management"]][["schedule"]]
# keep only first occurrence of each land_use
sched_nodup <- sched1[!duplicated(sched1$land_use), ]
# write back into covcrop
covcrop[[".data"]][["management"]][["schedule"]] <- sched_nodup
View(covcrop)
covcrop[[".data"]][["management"]][["schedule"]][["land_use"]]
#######################################################################
#### Probelm in CS5 and CS6: a lot of duplicates should be removed
load_farmr(paste(pth, 'covcrop.farm', sep = '/'))
# extract schedule
sched1 <- covcrop[[".data"]][["management"]][["schedule"]]
# keep only first occurrence of each land_use
sched_nodup <- sched1[!duplicated(sched1$land_use), ]
# write back into covcrop
covcrop[[".data"]][["management"]][["schedule"]] <- sched_nodup
statusquo$save()
covcrop$save()
load_farmr(paste(pth, 'covcrop.farm', sep = '/'))
library(SWATfarmR)
library(tidyverse)
library(zoo)
#---------------------------------------------------
# Define the path to your SWAT project
pth <- 'A://Users//Eini//CS6//6_NSWRM//FarmR//Covcrop//clean_setup'
# Define the path to your management schedule csv
mgt <- 'farmR_input_cc.csv'
load_farmr(paste(pth, 'covcrop.farm', sep = '/'))
View(covcrop)
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["lu_mgt"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["schedule"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["hru"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["schedule"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["schedule"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["lu_mgt"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["schedule"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["lu_mgt"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["lu_mgt"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["schedule"]]
load_farmr(paste(pth, 'covcrop.farm', sep = '/'))
# extract schedule
sched1 <- covcrop[[".data"]][["management"]][["schedule"]]
# keep only first occurrence of each land_use
sched_nodup <- sched1[!duplicated(sched1$land_use), ]
# write back into covcrop
covcrop[[".data"]][["management"]][["schedule"]] <- sched_nodup
covcrop[[".data"]][["management"]][["schedule"]]
covcrop[[".data"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["schedule"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["lu_mgt"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["lu_mgt"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["schedule"]]
#######################################################################
#### Probelm in CS5 and CS6: a lot of duplicates should be removed
#### sched must be similar or smaller than HRU numbers
load_farmr(paste(pth, 'covcrop.farm', sep = '/'))
# extract schedule
sched1 <- covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["lu_mgt"]]
# keep only first occurrence of each land_use
sched_nodup <- sched1[!duplicated(sched1$land_use), ]
# write back into covcrop
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["lu_mgt"]] <- sched_nodup
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["hru_name"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["lu_mgt"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["schedule"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["schedule"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["lu_mgt"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["lu_mgt"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["schedule"]]
# write back into covcrop
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["schedule"]] <- covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["lu_mgt"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["schedule"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["lu_mgt"]]
covcrop$save()
#---------------------------------------------------
# Load the farmR
library(SWATfarmR)
library(tidyverse)
library(zoo)
#---------------------------------------------------
# Define the path to your SWAT project
pth <- 'A://Users//Eini//CS6//6_NSWRM//FarmR//Covcrop//clean_setup'
# Define the path to your management schedule csv
mgt <- 'farmR_input_cc.csv'
load_farmr(paste(pth, 'covcrop.farm', sep = '/'))
new_farmr('covcrop', pth)
# Interpolate NA's in pcp data (if you have any)
covcrop$.data$variables$pcp[-1] <- na.approx(covcrop$.data$variables$pcp[-1])
# You can use by default the variables pcp, tmn, tmx, and tav, for dynamic
# rules in your management schedule. But you can also calculate and use new
# ones. You can also see in the example csv file how I used the variable
# in the dynamic rules. In this case I calculated the antecedent precipi-
# tation as a proxy for soil moisture using an exponential decay function
# (included in the R package). I used the pcp from the SWAT project for
# that which is stored in the farmR project.
api <- variable_decay(covcrop$.data$variables$pcp, -5,0.8)
# To add the variable to the farmR you have to tell it which variables are
# assigned to which HRUs for that I just copied the connections of pcp to
# HRUs and use the same for api
asgn <- select(covcrop$.data$meta$hru_var_connect, hru, pcp)
# You can add a variable to the farmR project with the internal function
# .$add_variable
covcrop$add_variable(api, "api", asgn, overwrite = T)
# Read your management table into your farmR project with .$read_management
covcrop$read_management(mgt, discard_schedule = TRUE)
# Study your management inputs
sched <- covcrop$.data$management$schedule
unique(sched$op_data1)
hru_attributes <- covcrop$.data$meta$hru_attributes
# You can now schedule operations which are based on your variables (pcp, etc.)
# and the rules that you defined in your management table. If no dates are
# defined the entire time series of your variables is used.
#farmR_ilm$schedule_operations(start_year = 2003, end_year = 2014, n_schedule=1) #n_schedule=1, => für jede landuse pro routing unit nur einen schedule in management.sch schreiben und für andere dann übernehmen)
covcrop$schedule_operations(start_year = 2001, end_year = 2021, replace = 'all')
#---------------------------------------------------
# Load the farmR
library(SWATfarmR)
library(tidyverse)
library(zoo)
#---------------------------------------------------
# Define the path to your SWAT project
pth <- 'A://Users//Eini//CS6//6_NSWRM//FarmR//Covcrop//clean_setup'
# Define the path to your management schedule csv
mgt <- 'farmR_input_cc.csv'
new_farmr('covcrop', pth)
# Interpolate NA's in pcp data (if you have any)
covcrop$.data$variables$pcp[-1] <- na.approx(covcrop$.data$variables$pcp[-1])
# You can use by default the variables pcp, tmn, tmx, and tav, for dynamic
# rules in your management schedule. But you can also calculate and use new
# ones. You can also see in the example csv file how I used the variable
# in the dynamic rules. In this case I calculated the antecedent precipi-
# tation as a proxy for soil moisture using an exponential decay function
# (included in the R package). I used the pcp from the SWAT project for
# that which is stored in the farmR project.
api <- variable_decay(covcrop$.data$variables$pcp, -5,0.8)
# To add the variable to the farmR you have to tell it which variables are
# assigned to which HRUs for that I just copied the connections of pcp to
# HRUs and use the same for api
asgn <- select(covcrop$.data$meta$hru_var_connect, hru, pcp)
# You can add a variable to the farmR project with the internal function
# .$add_variable
covcrop$add_variable(api, "api", asgn, overwrite = T)
# Read your management table into your farmR project with .$read_management
covcrop$read_management(mgt, discard_schedule = TRUE)
# Study your management inputs
sched <- covcrop$.data$management$schedule
unique(sched$op_data1)
hru_attributes <- covcrop$.data$meta$hru_attributes
# You can now schedule operations which are based on your variables (pcp, etc.)
# and the rules that you defined in your management table. If no dates are
# defined the entire time series of your variables is used.
#farmR_ilm$schedule_operations(start_year = 2003, end_year = 2014, n_schedule=1) #n_schedule=1, => für jede landuse pro routing unit nur einen schedule in management.sch schreiben und für andere dann übernehmen)
covcrop$schedule_operations(start_year = 2001, end_year = 2021, replace = 'all')
#---------------------------------------------------
# Load the farmR
library(SWATfarmR)
library(tidyverse)
library(zoo)
#---------------------------------------------------
# Define the path to your SWAT project
pth <- 'A://Users//Eini//CS6//6_NSWRM//FarmR//Covcrop//clean_setup'
# Define the path to your management schedule csv
mgt <- 'farmR_input_cc.csv'
new_farmr('covcrop', pth)
# Interpolate NA's in pcp data (if you have any)
covcrop$.data$variables$pcp[-1] <- na.approx(covcrop$.data$variables$pcp[-1])
# You can use by default the variables pcp, tmn, tmx, and tav, for dynamic
# rules in your management schedule. But you can also calculate and use new
# ones. You can also see in the example csv file how I used the variable
# in the dynamic rules. In this case I calculated the antecedent precipi-
# tation as a proxy for soil moisture using an exponential decay function
# (included in the R package). I used the pcp from the SWAT project for
# that which is stored in the farmR project.
api <- variable_decay(covcrop$.data$variables$pcp, -5,0.8)
# To add the variable to the farmR you have to tell it which variables are
# assigned to which HRUs for that I just copied the connections of pcp to
# HRUs and use the same for api
asgn <- select(covcrop$.data$meta$hru_var_connect, hru, pcp)
# You can add a variable to the farmR project with the internal function
# .$add_variable
covcrop$add_variable(api, "api", asgn, overwrite = T)
# Read your management table into your farmR project with .$read_management
covcrop$read_management(mgt, discard_schedule = TRUE)
# Study your management inputs
sched <- covcrop$.data$management$schedule
unique(sched$op_data1)
hru_attributes <- covcrop$.data$meta$hru_attributes
# You can now schedule operations which are based on your variables (pcp, etc.)
# and the rules that you defined in your management table. If no dates are
# defined the entire time series of your variables is used.
#farmR_ilm$schedule_operations(start_year = 2003, end_year = 2014, n_schedule=1) #n_schedule=1, => für jede landuse pro routing unit nur einen schedule in management.sch schreiben und für andere dann übernehmen)
covcrop$schedule_operations(start_year = 2001, end_year = 2021, replace = 'all')
View(covcrop)
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["schedule"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["lu_mgt"]]
covcrop[[".data"]][["scheduled_operations"]][["assigned_hrus"]][["n"]]
setwd("A:/Users/Eini/CS6/6_NSWRM/CoMOLA_SWATplus-main/models_2/SWAT")
### 1 - Load libraries and functions -------------------------------------------
source('calc_opt_indis.R')
source('calc_spi_indis.R')
source('./economic_model/CS_input_data.R')
library(data.table)
library(dplyr)
library(purrr)
library(readr)
library(stringr)
library(SWATmeasR)
### 2 - Define paths -----------------------------------------------------------
wd <- getwd()
txt_path <- paste0(wd,'/txt') # path to SWAT+ model txt folder
### 3 - Implement measures and run SWAT ---------------------------------
# Load the measR project which is located in the project path.
measr_file <- list.files(path = txt_path, pattern = '.measr$')
setwd("A:/Users/Eini/CS6/6_NSWRM/CoMOLA_SWATplus-main/models_2/SWAT")
### 1 - Load libraries and functions -------------------------------------------
source('calc_opt_indis.R')
source('calc_spi_indis.R')
source('./economic_model/CS_input_data.R')
library(data.table)
library(dplyr)
library(purrr)
library(readr)
library(stringr)
library(SWATmeasR)
### 2 - Define paths -----------------------------------------------------------
wd <- getwd()
txt_path <- paste0(wd,'/txt') # path to SWAT+ model txt folder
### 3 - Implement measures and run SWAT ---------------------------------
# Load the measR project which is located in the project path.
measr_file <- list.files(path = txt_path, pattern = '.measr$')
load_measr(paste0(txt_path, '/', measr_file))
# assign the data of the measr project with a specific name to the generic
# variable with the name 'measr'
assign('measr', get(gsub('.measr$', '', measr_file)))
# Reset SWAT files
measr$reset()
# Read genome
genome <- read.csv('genom.csv', header=T)
# Define HRUs subject of NSWRM implementation
idx <- which(genome == 2)
# Implement NSWRMs
if(is.integer0(idx) == F){
measr$implement_nswrm(nswrm_id = idx)
measr$write_swat_inputs()
}
# Run SWAT
set2wd(txt_path)
swat_exe <- list.files(txt_path, '.exe$')
system(swat_exe)
system(swat_exe)
#---------------------------------------------------
# Define the path to your SWAT project
pth <- './clean_setup'
# Define the path to your management schedule csv
mgt <- 'farmR_input.csv'
new_farmr('covcrop', pth)
#---------------------------------------------------
# Load the farmR
library(SWATfarmR)
library(tidyverse)
library(zoo)
#---------------------------------------------------
# Define the path to your SWAT project
pth <- './clean_setup'
# Define the path to your management schedule csv
mgt <- 'farmR_input.csv'
new_farmr('covcrop', pth)
# Interpolate NA's in pcp data (if you have any)
covcrop$.data$variables$pcp[-1] <- na.approx(covcrop$.data$variables$pcp[-1])
# You can use by default the variables pcp, tmn, tmx, and tav, for dynamic
# rules in your management schedule. But you can also calculate and use new
# ones. You can also see in the example csv file how I used the variable
# in the dynamic rules. In this case I calculated the antecedent precipi-
# tation as a proxy for soil moisture using an exponential decay function
# (included in the R package). I used the pcp from the SWAT project for
# that which is stored in the farmR project.
api <- variable_decay(covcrop$.data$variables$pcp, -5,0.8)
# To add the variable to the farmR you have to tell it which variables are
# assigned to which HRUs for that I just copied the connections of pcp to
# HRUs and use the same for api
asgn <- select(covcrop$.data$meta$hru_var_connect, hru, pcp)
# You can add a variable to the farmR project with the internal function
# .$add_variable
covcrop$add_variable(api, "api", asgn, overwrite = T)
# Read your management table into your farmR project with .$read_management
covcrop$read_management(mgt, discard_schedule = TRUE)
# Study your management inputs
sched <- covcrop$.data$management$schedule
unique(sched$op_data1)
hru_attributes <- covcrop$.data$meta$hru_attributes
# You can now schedule operations which are based on your variables (pcp, etc.)
# and the rules that you defined in your management table. If no dates are
# defined the entire time series of your variables is used.
#farmR_ilm$schedule_operations(start_year = 2003, end_year = 2014, n_schedule=1) #n_schedule=1, => für jede landuse pro routing unit nur einen schedule in management.sch schreiben und für andere dann übernehmen)
covcrop$schedule_operations(start_year = 2001, end_year = 2021, replace = 'all')
# You can now schedule operations which are based on your variables (pcp, etc.)
# and the rules that you defined in your management table. If no dates are
# defined the entire time series of your variables is used.
#farmR_ilm$schedule_operations(start_year = 2003, end_year = 2014, n_schedule=1)
#n_schedule=1, => für jede landuse pro routing unit nur einen schedule in management.sch schreiben und für andere dann übernehmen)
covcrop$schedule_operations(start_year = 1995, end_year = 2021, replace = 'all')
# When the scheduling was successful you can write the schedules into your
# SWAT project txt input files. Again you can do this for any time period.
# E.g. your calibration period and in another run for your validation period.
# When writing the schedules the simulation period of the model is changed
# accordingly, so that your schedules always match the weather time series.
# So do not change your simulation period manually. Then the simulated
# years will not match the calculated schedules.
covcrop$write_operations(start_year = 2001, end_year = 2021)
# Update land use labels (should not be longer than 24 characters)
library(SWATdoctR)
update_landuse_labels(pth)
# Reset files (required for rerunning the farmR)
#covcrop$reset_files()
